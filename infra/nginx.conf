events {}

http {
  server {
    listen 80;

    location /api/ {
      proxy_pass http://localhost:8000/;
      proxy_set_header Host $host;
    }

    location / {
      root /var/www/hackatec-residuos;
      try_files $uri $uri/ /index.html;
    }
  }
}
