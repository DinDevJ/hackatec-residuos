[Unit]
Description=Hackatec Residuos FastAPI
After=network.target

[Service]
User=www-data
WorkingDirectory=/opt/hackatec-residuos/backend
ExecStart=/usr/bin/uvicorn main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
